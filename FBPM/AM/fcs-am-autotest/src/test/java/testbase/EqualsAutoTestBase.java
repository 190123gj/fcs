/**
 * 
 */
package testbase;


/**
 * @author Peng
 * 
 */

public class EqualsAutoTestBase extends ServiceAutoTestBase {
	/*
	 * 该层主要负责构造对数据表对象校验方法
	 */
	
	//	/**
	//	 * p2pUserAccount
	//	 * @param
	//	 */
	//	protected void equalsP2pUserAccount(String partnerId, long id, String userId, Money balance,
	//										Money freezeBalance, String accountStatus,
	//										P2pUserAccountDO p2pUserAccountDo) {
	//		assertEquals(id, p2pUserAccountDo.getId());
	//		assertEquals(userId, p2pUserAccountDo.getUserId());
	//		assertEquals(balance, p2pUserAccountDo.getBalance());
	//		assertEquals(freezeBalance, p2pUserAccountDo.getFreezeBalance());
	//		assertEquals(partnerId, p2pUserAccountDo.getPartnerId());
	//		assertEquals(accountStatus, p2pUserAccountDo.getAccountStatus());
	//	}
	
}
