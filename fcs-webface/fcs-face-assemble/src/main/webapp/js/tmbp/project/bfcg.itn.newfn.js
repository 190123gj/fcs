define(function(require, exports, module) {

    var ValidataCommon = require('zyw/project/bfcg.itn.ValidataCommon')

    var new_a = $('.new-ui-tab a').length > 0 ? $('.new-ui-tab a') : ''

    if (!new_a) {
        return
    }

//    var ui_active = window.location.href.indexOf('&subIndex=1') > 0 ? 1 : 0

//    new_a.removeClass('active').eq(ui_active).addClass('active')

})